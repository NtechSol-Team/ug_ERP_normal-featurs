## Packages
recharts | Dashboard charts and analytics visualization
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes

## Notes
Integration assumptions:
- Auth uses cookie-based session (credentials: include)
- Dashboard stats endpoint returns consolidated numbers
- Role-based access control (RBAC) handled via user object in auth context
